function getMatchingFlights(e){return flights.filter(function(t){return t.origin===e.origin&&t.destination===e.destination})}var express=require("express"),http=require("http"),airports=require("./data/airports.json"),flights=require("./data/flights.json"),reservations=[],app=express().use(express.bodyParser()).use(express.static("public"));for(var i=0;i<flights.length;i++){flights[i].originFullName=airports[flights[i].origin].name;flights[i].destinationFullName=airports[flights[i].destination].name}app.get("/airports",function(e,t){t.json(airports)});http.createServer(app).listen(3e3,function(){console.log("Server ready at http://localhost:3000")});